{
  "name": "clock-out-backend-lambda",
  "version": "1.0.0",
  "description": "backend services for the Clock Out app",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "dotenv": "^16.4.7"
  }
}
